# Technical and Conceptual Debt

## Active Debt

| ID | Area | Problem | Impact | Suggested Resolution | Status |
|---|---|---|---|---|---|
| Q001 | Trigger model — Model B | Mechanism resolved (Option 3 / D007). Compaction hook defined (D006). Remaining open: full skill prompt design for proactive Model B recognition beyond compaction — when should the agent volunteer AIPCS without a user hint or compaction event? | v1 relies on explicit hint + compaction; fully autonomous proactive triggering is deferred | Design and test the Model B recognition heuristics; document in skill definition | Open (partial) |
| Q004 | Multi-agent locking | No locking model defined for concurrent access to the same Domain Service by multiple agents | Risk of write conflicts and data corruption in multi-agent/productised scenarios | Define locking strategy during hosted/productisation work; SQLite WAL mode may be sufficient for local v1 | Deferred productisation |
| Q005 | Schema conflict resolution | Additive evolution is implemented and destructive-equivalent repair is possible through create/update/delete/history; bulk/destructive helpers remain undefined | Productised/compliance workflows may need safer bulk operations, backfills, and stronger audit controls | Keep research surface autonomy-first; revisit helpers and compliance controls later | Open (partial) |
| Q009 | Domain taxonomy — registry model | Common domain-class guidance exists, but immutable externally registered seeds and application/vendor/IT constraints are not modelled | Productised systems may need stable anchors while agent-owned memory needs reclassification autonomy | Keep non-binding classes for research; define immutable/external registration semantics later | Open (partial) |
| Q011 | Tier 3 access scope | Tier 3 elevated access is designed but not implemented | Consent/export personas remain productisation objectives, but not paper-minimum work | Defer implementation until productisation/access-control phase | Deferred productisation |
| Q012 | Server-controlled fields | Server-managed field handling is clearer in record tools and schema evolution, but first-design manifests still represent these fields as attributes | Causes validation friction and encourages agents to route around the tool contract if filesystem access is available | Decide whether first-design manifests should continue listing server fields or move them into explicit metadata | Open (partial) |
| Q026 | Session-start retrieval discipline | Bootstrap exposes memory shape but agents may fail to retrieve critical low-cardinality content such as user identity or behavioral rules | Agent can falsely claim not to know persisted facts even though bootstrap showed where they are | Define skill guidance and eval cases for bootstrap → bounded content retrieval | Open |
| Q027 | Bootstrap instruction layer | The dynamic bootstrap tool cannot trigger itself; agents need static AIPCS instructions at session start | User must keep telling the agent AIPCS exists before discovery happens | Package portable AIPCS skill/instructions for local Claude/Codex and later hosted clients | Open |
| Q028 | Common domain-class definitions | `domain_class` is useful for alignment, but no reference definitions exist for common use cases | Agents may create semantically overlapping top-level categories across systems | Define non-binding common domain classes with descriptions; avoid closed taxonomy enforcement in v1 | Open |
| Q029 | Procedural skills boundary | Some multi-step AIPCS workflows may be better stored as skills than tools, but the boundary is undefined | Risk of overloading atomic tools with workflow policy or storing procedures as ordinary records | Define criteria for tool vs skill vs record once recurring multi-step operations emerge | Open |
| Q030 | Related memory baselines | `memhub` and other open-source systems are relevant, but running multiple external comparators could expand scope | Paper may under-characterise related work if ignored, or lose focus if too many baselines are added | Cite `memhub` as related work for now; use `agent-memory-v2` as owned comparator unless reviewer risk requires more | Resolved for first paper |
| Q045 | Longitudinal retrieval pressure | It is unclear whether larger volumes of persisted records naturally push agents toward better retrieval-oriented schemas | Volume alone may produce better schema self-audit, or it may simply produce more prose blobs and broader retrieval | Add a later longitudinal evaluation: repeated sessions, growing record count, periodic schema self-audit, and retrieval-quality trend review | Open |
| Q046 | Vendor/harness opacity | Hosted agent vendors can change model routing, system prompts, or tool policies without full visibility | Longitudinal benchmark comparisons may become invalid if run far apart | Record date, visible model label, tool surface, prompt, transcript, and run scenarios close together; report as agent-harness behavior, not model-internal fact | Open |
| Q047 | Graph/database substrate scope | Graph/vector databases may be useful substrates, but they do not answer who owns and evolves the memory architecture | Could distract from the core hypothesis and add compute/ops cost before it is needed | Keep SQLite/structured local store for the first paper; revisit richer stores only as substrate comparisons after the architecture claim is evaluated | Open |
| Q048 | Memory maintenance tools | The agent should be able to discover ageing, stale, low-activity, duplicate, or prune/archive candidates, but the first helper surface is undefined | Without lightweight maintenance discovery, memory quality may depend on manual prompting rather than agent-led upkeep | Design the smallest maintenance discovery tool after agent-led evaluation exposes which signal is most useful | Open |
| Q049 | Administrative expunge audit | Productised/compliance controls may remove records outside the normal agent memory plane, but the residual signal visible to the agent is undefined | Silent removal could confuse future recall; over-disclosure could conflict with compliance/legal requirements | Define a transparent audit/history/log convention for externally removed records where legally and operationally appropriate | Open |
| Q050 | Immutable external seeds | Externally registered application/vendor/IT/compliance seeds may need to remain stable while agent-owned services need reclassification autonomy | A fixed seed could preserve integration contracts but constrain agent memory evolution | Define immutable seed semantics as a productisation control-plane feature, distinct from normal agent-owned reclassification | Open |
| Q033 | Schema self-audit rubric | A live Claude trace showed useful self-restructuring, but there is no objective scoring rubric for retrieval-oriented schema repair | Without a rubric, agent churn could be mistaken for schema improvement | Define evaluation criteria: granularity, queryability, lifecycle expression, authority boundaries, provenance quality, and bounded change count | Open |
| Q034 | Duplicate authority convention | AIPCS services can accidentally duplicate facts already owned by another service | Drift becomes likely when summaries and structured records both claim authority over the same state | Define conventions for summary/cache/derived-view records and when they are allowed | Open |
| Q035 | Unreliable required fields | Agent-authored required fields can later appear suspicious, but additive-only evolution cannot easily null, deprecate, or correct them | Bad required fields may remain in schemas and records even after an agent detects weak provenance | Define confidence/deprecation/correction semantics for agent-authored fields and destructive/backfill migrations | Open |
| Q036 | Session rationale schema | Session records are emerging as the right place for why a schema or memory policy changed, but no minimal schema has been standardised | Session records could become transcript blobs or inconsistent notes, undermining retrieval | Define minimal fields for session rationale: topic, summary, decisions, schema_implications, retrieval_guidance, source/provenance | Open |
| Q037 | Rationale discovery | Bootstrap is content-free, but agents need enough signal to know session rationale exists and is worth retrieving | Future agents may inspect migration history but miss the session record that explains why the migration happened | Consider recent-activity/retrieval hints for session/rationale entities without exposing content | Open |
| Q038 | Authority-layer instruction | The portable AIPCS instruction artifact does not yet explicitly define the authority split between static instructions, bootstrap, migration history, session records, and behavioral memory | Agents may overuse static files or bootstrap for content that belongs in AIPCS records | Update instruction artifact after the session-rationale pattern stabilises | Open |
| Q018 | Interpretation policy | No fixed policy exists for weighting provenance, stale records, or old inferences | Different agents may apply inconsistent recall behavior, but fixed policy would weaken agent/user autonomy | Provide hints/conventions in instructions and metadata; let agent and user decide in context | Resolved for research phase |
| Q019 | Bootstrap export | The minimum portable state for an agent that starts without a live AIPCS connection is undefined | Risk of dual-store drift between file memory and AIPCS content | Define bootstrap export as a shape/map only, not full content duplication | Open |
| Q022 | Session identity | Session is useful for evaluation and trace analysis, but may not be a first-class AIPCS concept | Forcing session semantics into every service could over-prescribe agent-owned memory architecture | Keep session/rationale as an evaluation and agent-chosen memory pattern; avoid making it mandatory until stronger evidence appears | Open (partial) |
| Q025 | Tool boundary enforcement | Local development agents with filesystem access can bypass MCP tools and write SQLite directly | This undermines server-managed fields, auditability, validation, and security claims | Treat direct DB access as out-of-contract in eval; isolate DB files in homelab/hosted deployments | Deferred productisation |
| Q024 | Public MCP transport | Hosted ChatGPT/Claude-style clients may require publicly reachable MCP or a bridge, unlike local `stdio` clients | Roadmap can overestimate what local MCP proves for hosted clients | Keep local, homelab/private, and public-hosted transport phases distinct | Deferred productisation |

## Resolved

| ID | Area | Resolution | Date |
|---|---|---|---|
| Q001 (mechanism) | Mechanism — CLI vs sidecar vs MCP-native | Option 3 selected: AIPCS as MCP-native primitive server. No CLI, no sidecar HTTP API. (D007) | 2026-05-04 |
| Q001 (compaction trigger) | Proactive trigger — compaction hook | Compaction identified as primary Model B trigger. Skill includes compaction hook guidance. (D006) | 2026-05-04 |
| Q002 | Schema versioning format | Schema manifest format defined in `docs/AIPCS_v1_Technical_Design.md` — versioned JSON, travels with the service, includes migration history. | 2026-05-04 |
| Q003 | Service registry / discovery | `aipcs_service_list` primitive + Registry DB in AIPCS Server. Agent calls list before seeding to avoid duplicates. | 2026-05-04 |
| Q006 | Portability | `aipcs_service_export` primitive (json / sqlite / schema_only / data_only / full) provides the portability primitive. Full cross-deployment import format TBD. | 2026-05-04 |
| Q014 | Search/retrieval shape | Dedicated `aipcs_record_search` uses exact structured filters only for the first retrieval slice. Partial text, fuzzy, FTS, embedding, and cross-service search are deferred. | 2026-05-18 |
| Q016 | Retrieval metadata | Retrieval responses from list/get/search return `_meta` with `computed_at`, `updated_age_seconds`, `updated_age_label`, and provenance when convention fields are present. | 2026-05-18 |
| Q017 | Provenance vocabulary | Recommended schema convention defined: `provenance_type`, `provenance_note`, `provenance_source`, with type values `user_stated`, `agent_inferred`, `agent_observed`, `imported`. | 2026-05-18 |
| Q021 | Bootstrap tool boundary | Dedicated `aipcs_bootstrap` selected for the first session-start shape surface instead of overloading `aipcs_service_list`. | 2026-05-18 |
| D026 | Paper-minimum productisation scope | Minimum paper evidence does not require homelab hosting, OAuth/DCR implementation, public MCP, graph DB, or production hardening. Local Python/SQLite/MCP plus deterministic and live-agent evidence is sufficient for bounded research claims. | 2026-05-19 |
| D026 | Dynamic generated tool requirement | Generated domain-specific MCP tools are optional UX/productisation for V1, not the core proof. Stable primitive MCP tools are sufficient to evaluate agent-owned schema design, persistence, retrieval, and evolution. | 2026-05-19 |
| Q007 | Seed payload | Minimum seed payload is `domain_name`, `domain_class`, and `intent_description`; server owns timestamps, state, owner, and audit metadata. | 2026-05-19 |
| Q008 | Seed TTL | Do not auto-expire seeds in the normal agent memory plane. Prefer agent-facing maintenance tools; administrative expiry/expunge belongs to productisation/compliance control planes with audit where possible. | 2026-05-19 |
| Q010 | Domain overlap | Use broad `domain_class`, precise `domain_name`, non-binding guidance, and agent-managed reclassification for research phase. | 2026-05-19 |
| Q013 | Session bootstrap | Dedicated `aipcs_bootstrap` plus static agent instruction supersedes session-start `aipcs_service_list`. | 2026-05-19 |
| Q018 | Provenance/staleness weighting | Fixed weighting policy is not standardised for research phase; agent and user decide using provenance, recency, and instruction hints. | 2026-05-19 |

## Cleanup Rules

- Close an entry when the question is resolved — add to the Resolved table with the decision reference
- Promote recurring questions into harness doc rules rather than leaving them as repeated debt
- Record any new conceptual or technical gaps discovered during the build
