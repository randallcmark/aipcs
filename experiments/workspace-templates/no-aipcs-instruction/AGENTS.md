# Experiment Workspace: No AIPCS Instruction

This is a control workspace.

Do normal agent work from the user's instructions. Do not assume a persistent memory system exists unless a tool or user instruction makes it visible during the session.
